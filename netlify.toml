# ✅ Netlify config for Next.js with middleware and subdomains

[build]
  command = "npm run build"
  # ✅ DO NOT SET publish — plugin will manage it!

[build.environment]
  NODE_VERSION = "20"
  NEXT_TELEMETRY_DISABLED = "1"

# ✅ Enable Next.js runtime + middleware
[[plugins]]
  package = "@netlify/plugin-nextjs"

# ✅ Security headers
[[headers]]
  for = "/*"
  [headers.values]
    X-Frame-Options = "DENY"
    X-XSS-Protection = "1; mode=block"
    X-Content-Type-Options = "nosniff"
    Referrer-Policy = "strict-origin-when-cross-origin"

# ✅ Cache static assets
[[headers]]
  for = "/_next/static/*"
  [headers.values]
    Cache-Control = "public, max-age=31536000, immutable"

# ✅ Functions bundling
[functions]
  node_bundler = "esbuild"
